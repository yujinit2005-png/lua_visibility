# LUVIS AI 가시성 진단기 - CHANGELOG

모든 중요 변경사항과 기능 추가, 버그 수정 내역을 일자별로 기록합니다.

---

## [v1.0.14] - 2026-08-12

### ⚙️ AI 진단 분리, 실패 질문 덮어쓰기 재실행 및 재생성 오류 수정 (GUI & Core)
- **1회당 제한시간 기본값 상향 (1000초)**: `limit_sec` 기본값을 600초에서 1000초로 변경.
- **AI 진단 실행과 PDF 생성 기능 분리**:
  - `🚀 AI 가시성 진단 실행`: AI 수집 및 2x2 기회지도 분석/DB 저장만 진행 (PDF 자동 렌더링 제외).
  - `📄 영업용 진단 PDF 생성`: 최신/선택된 DB Run 데이터로 독립적 PDF 리포트 출력.
- **실패 질문 개별/일괄 재실행 & 기존 진단 SEQ 덮어쓰기 (UPDATE)**:
  - `Storage.overwrite_answer()` 및 `recalculate_run_stats()` 신설. 재실행 시 기존 `run_id` 레코드를 UPDATE로 덮어쓰고 성공률/노출률 자동 재산출.
  - 교차 비교 창 내 질문별 `⚡ 개별 재실행` 및 `⚡ 실패 질문 일괄 재실행` 버튼 구현.
- **from-run 재생성 임시 차단 오류 수정 (`regenerate.py`)**: `client+precision` 모드 재생성 시 무조건 차단(exit code 2)되던 레거시 코드 제거로 PDF 재생성 완벽 지원.

---

## [v1.0.13] - 2026-08-12

### 🌐 다중 PC 환경 PDF 생성 및 내장 뷰어 브라우저 호환성 강화 (Core & Verifier)
- **Playwright 바이너리 다중 론처 보강 (`sales_pdf.py`, `web_verifier.py`)**: Playwright Chromium 바이너리가 설치되지 않은 타 PC/배포 환경에서도 Windows 기본 MS Edge(`channel="msedge"`) 및 Chrome(`channel="chrome"`) 브라우저를 자동 탐지하여 1차 PDF 변환 및 내장 뷰어 실측 창이 정상 작동하도록 개선.
- **2차 헤드리스 PDF 폴백 고도화**: MS Edge 검색 경로를 사용자 계정 폴더(`%LOCALAPPDATA%`) 및 32/64비트 Program Files로 확장하고, 최신 Edge `--headless=new` 및 `--no-sandbox` CLI 옵션 적용 및 구버전 CLI 자동 재시도 로직 구현.
- **내장 뷰어/순차 자동 실측 미열림 현상 방지**: 브라우저 론칭이 차단되는 사내 보안 환경일지라도 시스템 기본 웹 브라우저(`webbrowser.open`)로 해당 URL 탭을 강제 오픈하는 예외 처리 수록.
- **실행파일(.exe) 재컴파일 완료**: `build.py` 패키징 완료 (`LUVIS_AI가시성진단기.exe`, 55.6MB).

### 🚀 웹 마이그레이션 종합 기획 명세서 작성 (Docs & Architecture)
- **`Mig_to_Web.md` 신설**: Python (Tkinter + SQLite + Playwright) 기반 소스코드를 React (TypeScript) + Supabase + Cloudflare 사양으로 이관하기 위한 5단계 기술 명세서 및 AI 에이전트 전용 한방 실행 프롬프트 작성.

---

## [v1.0.12] - 2026-08-11

### 📝 Trust Signal 리포트 상세 설명(가이드) 블록 추가 (Report Design)
- **상세 설명 섹션 신설**: Trust Signal Audit 페이지 하단에 `Trust Signal 핵심 평가 지표 가이드` 섹션(F)을 추가하여, 단답형으로 제공되던 점검 결과에 대한 원장님(고객)의 이해도를 높였습니다.
- **4대 핵심 지표 그리드 UI**: AI 크롤러 허용(robots.txt), 병원 규격 데이터(Schema), 전문성 입증 자산(콘텐츠), 웹 기초 가독성(Technical) 등 4가지 주요 평가 항목의 중요성과 개선 시 기대효과를 2x2 카드 그리드 형태로 상세히 서술하여, 별도의 구두 설명 없이도 리포트가 스스로 충분한 설득력을 갖도록 개선했습니다.



## [v1.0.11] - 2026-08-11

### 📊 AEO 노출분석 리포트 경쟁병원 비교 지표 추가 (Report & Analytics)
- **주요 경쟁병원 대비 전체 노출도 비교 섹션(B) 신설**: AEO 노출분석 페이지에 당원 노출률과 상위 2개 경쟁병원의 전체 평균 노출률을 비교하는 가로 막대 그래프 추가.
- **동적 비교 브리핑 텍스트**: 우리 병원과 1위 경쟁병원의 노출 점유율 격차(우위/경합/열세)에 따라, 원장님이 현재 AEO 포지셔닝을 직관적으로 이해할 수 있는 맞춤형 브리핑 문구가 자동으로 생성되도록 고도화.



## [v1.0.10] - 2026-08-11

### 🐛 리포트 재생성 시 GEO 리포트 미생성(누락) 버그 수정 (Core & DB)
- **DB 스키마 확장**: `runs` 테이블에 `trust_report_json` 컬럼을 신설하여, 최초 실측 시 점검한 병원 홈페이지 신뢰도(Trust Signal) 분석 결과를 JSON 형태로 영구 보존하도록 조치.
- **오프라인 복원 로직 구현**: `regenerate.py`가 DB에서 과거 런(run) 데이터를 불러올 때, 저장된 `trust_report_json`을 역직렬화(`TrustSignalReport.from_dict`)하여 리포트 엔진에 주입하도록 개선.
- **결과**: 외부 네트워크 재접속 없이도 과거 측정 당시의 홈페이지 점검 상태가 완벽하게 복원되며, GEO 리포트 섹션이 '미점검' 처리되지 않고 정상 생성됨.



## [v1.0.9] - 2026-08-11

### 📑 GEO 리포트 다중 취약 항목 브리핑 지원 (Report)
- **복수 미달 항목 동시 노출**: 기존에는 여러 항목(예: B항목과 C항목)이 동시에 미달될 경우 가장 배점이 큰 1개 항목에 대해서만 브리핑 박스가 생성되었으나, 이제 **미달된 주요 항목을 최대 2개까지 연속해서 브리핑**하도록 개선.
- **총평(Interp) 문구 최적화**: 복수 항목 미달 시, 각 항목의 성격(홈페이지 설정, 콘텐츠 보완)을 조합하여 "홈페이지 설정 및 콘텐츠 보완 복합 작업에 해당하므로..." 와 같이 문맥이 자연스럽게 이어지도록 동적 텍스트 결합 로직 고도화.

---

## [v1.0.8] - 2026-08-11

### 📝 GEO 리포트 미달 항목 브리핑 전면 보강 (Report & UX)
- **C 항목(신뢰 콘텐츠 자산) 전용 브리핑 추가**: C 항목이 미달일 때 원장님이 직관적으로 이해할 수 있도록 '전문 콘텐츠 부족으로 인한 추천 배제' 및 '콘텐츠 가이드를 통한 단계적 보완' 메시지를 구체적으로 명시.
- **D 항목(기술적 AI 가독성) 전용 브리핑 추가**: D 항목이 미달일 때 '이미지 위주 구성으로 인한 AI 인식 불가 현상' 및 '기초 웹 기술 표준화 조치'의 필요성을 원장님 친화적인 언어로 명시.
- 기존 A, B 항목 외에 나머지 항목들에 대해서도 모두 맞춤형 원인 분석과 해결책이 생성되도록 리포트(`sales_pdf.py`) 생성 로직 개선.

---

## [v1.0.7] - 2026-08-11

### 🛑 측정 중단 버튼 즉각적 UI 반응 추가 (UX 개선)
- **질문 단위 중간 대기 상태 알림 추가**: 기존 `[v1.0.4]`의 질문 단위 중단 로직은 완벽히 동작했으나, 질문 측정 반복(Repetition) 도중 버튼을 눌렀을 때 남은 반복이 끝날 때까지 콘솔에 즉각적인 반응이 없어 '멈춘 것 같은(아무 반응 없는)' 오해를 주던 현상 해결.
- 이제 질문 반복 중에 `[측정 중단]`을 누르면 **즉시 콘솔에 "[사용자 중단 요청 수신] 현재 진행 중인 질문의 남은 측정을 완료한 뒤 안전하게 중단됩니다..."** 라고 응답을 주며, 실제 중단 적용은 원래 요청대로 현재 질문 측정 수집이 모두 종료된 후 다음 질문으로 넘어가기 직전에 정상적으로 `Skip` 처리됩니다 (`run.py`).

---

## [v1.0.6] - 2026-08-11

### 🐛 버그 수정 (Bug Fixes)
- **GUI [측정 중단] 버튼 이벤트 핸들러 수정**: 스레드 모드 진단 실행 시 `proc["p"]` 미설정으로 인해 [측정 중단] 버튼 클릭 이벤트가 무시되던 버그 수정. 진단 실행 상태(`proc["running"]`) 감지 및 `.cancel_request` 신호 파일 생성을 정상화하여 질문 단위 안전 중단 기능이 완벽 동작하도록 개선 (`lua_gui.pyw`).

---

## [v1.0.5] - 2026-08-11

### 📋 DB 로그 결과 저장 확장 (Database & Logging)
- **`runs.result_summary` 저장 내용 확장**: 단순 무결성 요약 문구 대신, 실시간 진행 프로그레스 콘솔 로그 전체(실행 계획, AI 모델별 진단 진행, 질문별 노출률/상태, 무결성 통과 여부)를 `result_summary` 컬럼에 그대로 보관하도록 개선 (`run.py`).

---

## [v1.0.4] - 2026-08-11

### 🛑 중단 제어 및 UX 시각화 개선 (Control & UI)
- **측정 중단 버튼 색상 변경**: 기존 회색(`GRAY_BTN`)에서 시인성이 높은 강렬한 크림슨 레드(`#D9381E`)로 도드라지게 변경.
- **질문 단위 중단 로직 개선**: 기존 단일 task 중간 중단 방식에서, 진행 중인 **질문의 모든 반복(Repetition) 측정이 끝난 후 다음 질문으로 넘어가기 직전에 중단**하도록 안전 정리 범위 변경 (`run.py`).

---

## [v1.0.3] - 2026-08-11

### 🤖 AI 진단 도구 및 정밀 옵션 제어 (GUI & Execution)
- **AI 진단 도구 선택 체크박스 구현**: 4개 AI 진단 도구(OpenAI ChatGPT, Google Gemini, Perplexity, Anthropic Claude) 체크박스 추가. `.env`에 키가 존재할 경우 기본 선택, 키가 없는 경우 비활성화(Disabled) 처리.
- **진단 횟수 및 제한시간 동적 제어**: 
  - `무료 AI 진단`: 진단 횟수 1회 고정, 1회당 제한시간 600초 고정.
  - `정밀 AI 리포트`: 진단 횟수를 1회~10회 콤보박스 선택 가능, 1회당 제한시간(초) 사용자 지정 가능(기본값 600초).
- **CLI 연동 확장**: `--enabled-models`, `--repetitions`, `--limit-sec` 플래그를 통해 GUI의 설정값이 코어 실행 엔진(`run.py`)에 동적으로 반영되도록 개선.

---

## [v1.0.2] - 2026-08-11

### 🔒 보안 및 환경설정 (Environment & Security)
- **.env 환경변수 관리 도입**: `keys.json` 대신 `.env` 파일에서 API 키(`OPENAI_API_KEY`, `GEMINI_API_KEY` 등)를 로드하고 저장하도록 전환 (`lua_visibility/env_loader.py` 신설).
- **보안 무결성 강화 (.gitignore 신설)**: API 키가 담긴 `.env`, `keys.json` 파일 및 내부 실행 산출물(`lua_visibility.db`, `Audit/`, `Report/`, `dist/`, `build/`)이 Git 업로드에 노출되지 않도록 업로드 제외 규칙 적용.

### 📊 DB 측정 현황 및 실시간 기록 (Database & Audit)
- **runs 테이블 스키마 확장**: 실시간 진단 상태 및 로그 저장을 위해 `status`, `total_tasks`, `completed_tasks`, `success_rate`, `overall_mention_rate`, `result_summary`, `ended_at` 컬럼 추가.
- **실시간 진단 로그 기록**: 진단 시작 시 `RUNNING` 상태 기록, Task별 진행률(`completed_tasks`) 실시간 갱신, PDF 생성 직전 최종 결과 업데이트.
- **신윤수내과의원.json DB 시딩**: 병원 마스타 정보(`HOSP_006`) 및 질문세트(`v1.0`) SQLite DB 동기화 완료.

---

## [v1.0.1] - 2026-07-30

### 🚀 v0.1 안정화 및 리포트 품질 보완
- **안정성 강화**: 요청 타임아웃 90초 설정, 재시도 1회 제한(429/503 전용), GUI [측정 중단] 안전 종료 설계.
- **데이터 무결성 게이트**: 성공률 80% 미만 시 무결성 미달 표기 및 PDF 경고 메시지 렌더링.
- **병원 마스타 & 질문 세트 버저닝**: `hospitals` 및 `hospital_config_versions` 테이블 도입 및 `Hospital_info/*.json` 양방향 시딩 모듈 구축.
